package switchisep.project.dto;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SprintDTOTest {

    @Test
    void sprintDTOConstructor(){

        SprintDTO sprintDTO = new SprintDTO();

        assertEquals(sprintDTO, sprintDTO);
    }

}