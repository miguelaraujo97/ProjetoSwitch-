package switchisep.project.services;

import org.mockito.Mockito;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Primary;
import switchisep.project.domain.project.ProjectInterfaceFactory;


//@Profile("Service")
@Configuration

public class ConfigTestService {

        /*@Bean
        @Primary
        public ProjectStore ProjectStore() {
            return Mockito.mock(ProjectStore.class);
        }

        @Bean
        @Primary
        public ProjectInterfaceFactory ProjectInterfaceFactory() {
            return Mockito.mock(ProjectInterfaceFactory.class);
        }*/
    }


